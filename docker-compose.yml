version: '3'

services:
  back-end:
    build: ../../back-end
    volumes:
      - ../../back-end:/usr/src/app
    ports:
      - "3001:3000"
  front-end:
    build: .
    volumes:
      - ../my-app:/usr/src/app
    depends_on:
      - back-end
    ports:
      - "8888:80"